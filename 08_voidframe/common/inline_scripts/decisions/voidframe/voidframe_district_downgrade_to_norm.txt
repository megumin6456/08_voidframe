decision_giga_voidframe_$type$_downgrade_no = {
	# this means downgrade to tier
	# THE NAME IS THE TARGET
	icon = decision_giga_voidframe_$type$_no
	owned_planets_only = yes

	# SCRIPT INPUTS
	# type = district type
	# cost = cost of upgrading

	potential = {
		# standard frame checks
		exists = owner
		owner = {
			giga_has_frameworld_origin = yes
		}
		uses_district_set = giga_frameworld

		# no need to check for frame lvl, we are downgrading

		# check if we have the flag to downgrade from
		has_planet_flag = anz_voidframe_$type$_dm
		
		has_planet_flag = anz_voidframe_show_downgrade
	}

	# no reqtech, we are downgrading

	resources = {
		category = megastructures
		cost = {
			$cost$
		}
	}

	effect = {
		custom_tooltip = decision_giga_voidframe_$type$_downgrade_no_effect
		hidden_effect = {
			remove_planet_flag = anz_voidframe_$type$_dm
			validate_planet_buildings_and_districts = yes
		}
	}
	
	ai_weight = {
		weight = 10
	}
}